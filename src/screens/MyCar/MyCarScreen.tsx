import { View, Text } from 'react-native';

export default function MyCarScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>My Car Screen</Text>
    </View>
  );
}